#! /bin/bash

ARG="$@"

if [ "x${ARG}" = "x" ]; then
	echo -e "[ERROR] -- no string as argument\n"
	exit
else
	FNAME=$ARG
fi

echo -n "Converting 4 spaces to tabs for file ${FNAME} ... "
sed -i 's/ \{4\}/\t/g' ${FNAME}
echo "[OK]."

