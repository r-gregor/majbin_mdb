#! /bin/bash

# echo "<filename>" | tr ' ' '_'

ARG="$@"

if [ "x${ARG}" = "x" ]; then
	echo -e "[ERROR] -- no string as argument\n"
	exit
else
	STRNG=$ARG
fi

OUTPUT=$(echo ${STRNG} | sed 's/ \././'  | tr ' ' '_')
echo "${OUTPUT}" | sed 's/"//g'

